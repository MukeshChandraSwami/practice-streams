package com.learn.streams.controller;

import org.springframework.web.bind.annotation.RestController;

import static com.learn.streams.constants.ApiConstants.BASE_URL_STREAM;
import static com.learn.streams.constants.ApiConstants.CURRENT_VERSION;

@RestController(BASE_URL_STREAM +CURRENT_VERSION)
public class StreamController {


}
